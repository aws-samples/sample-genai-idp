# Build artifacts (regenerated by package_agent_source.sh / idp-feature-cli)
agent-source.zip
# Staged accelerator library copied into the build context at package time.
idp_common_pkg/
feature-ui/dist/
feature-ui/node_modules/
.aws-sam/
__pycache__/
*.pyc
