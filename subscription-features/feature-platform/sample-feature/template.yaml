AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  docs-by-status feature stack — sample implementation of the IDP Accelerator
  Feature Platform contract. Adds a "Docs By Status" page to the IDP UI that
  reads counts from the host's TrackingTable via a scoped read policy.

Parameters:

  MainStackName:
    Type: String

  # NOTE: There is intentionally no FeatureVersion parameter. The version is
  # baked into this template at publish time by `idp-feature-cli publish`,
  # which substitutes the literal string `<FEATURE_VERSION_TOKEN>` (see below)
  # with the value from feature.yaml -> version. Why bake it in instead of
  # using a CFN Parameter? CloudFormation Console's "Update stack" wizard
  # ignores `param_*` query-string overrides — admins clicking "Update" from
  # the IDP nav would otherwise stay on the old version. By making the new
  # template literally contain the new version string, CFN sees a real
  # template change and the update applies cleanly without UI gymnastics.

  FeatureId:
    Type: String
    Default: docs-by-status

  FeatureDisplayName:
    Type: String
    Default: DemoFeature - Docs By Status

  SellerBucket:
    Type: String

  SellerBucketRegion:
    Type: String
    Default: us-east-1

  LogLevel:
    Type: String
    Default: INFO
    AllowedValues: [DEBUG, INFO, WARNING, ERROR]

Globals:

  Function:
    Runtime: python3.12
    Timeout: 60
    MemorySize: 256
    Tracing: Active
    Architectures: [arm64]

Resources:

  ###########################################################################
  # Feature API: HttpApi + Lambda, authorised by main stack's Cognito User Pool.
  ###########################################################################
  FeatureApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      # CORS is served by API Gateway's built-in preflight handler. Without
      # this block, OPTIONS preflight requests hit the default JWT authorizer,
      # which rejects them (no Authorization header on preflight) and the
      # browser shows "Failed to fetch" before it can ever send the GET.
      # API Gateway HTTP APIs handle OPTIONS entirely inside the gateway when
      # CorsConfiguration is set — preflight never reaches the authorizer or
      # the backend Lambda.
      #
      # AllowOrigins='*' is safe here because the API uses Bearer JWT
      # authentication (Authorization header), NOT cookies. The browser's
      # same-origin policy only restricts credentialed requests; with
      # AllowCredentials unset (default false), wildcard origin is fine and
      # CORS-compliant. Switching to a specific origin would require the host
      # to export its CloudFront domain name, which it currently does not.
      CorsConfiguration:
        AllowOrigins:
          - '*'
        AllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS
        AllowHeaders:
          - authorization
          - content-type
          - x-amz-date
          - x-amz-security-token
        MaxAge: 600
      Auth:
        DefaultAuthorizer: CognitoJwt
        Authorizers:
          CognitoJwt:
            JwtConfiguration:
              issuer: !Sub
                - 'https://cognito-idp.${AWS::Region}.amazonaws.com/${UserPoolId}'
                - UserPoolId: { 'Fn::ImportValue': !Sub '${MainStackName}-UserPoolId' }
              audience:
                - !Sub
                  - '${ClientId}'
                  - ClientId: { 'Fn::ImportValue': !Sub '${MainStackName}-UserPoolClientId' }
            IdentitySource: '$request.header.Authorization'

  FeatureApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: feature-api/
      Handler: handler.lambda_handler
      Environment:
        Variables:
          LOG_LEVEL: !Ref LogLevel
          MAIN_STACK_NAME: !Ref MainStackName
          # Read the host's TrackingTable name from its Exports. The main stack
          # re-exports this from its feature-platform nested stack as
          # `${MainStackName}-TrackingTableName`.
          DOCUMENTS_TABLE_NAME:
            'Fn::ImportValue': !Sub '${MainStackName}-TrackingTableName'
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - dynamodb:Scan
                - dynamodb:Query
              Resource:
                - !Sub
                  - 'arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${T}'
                  - T: { 'Fn::ImportValue': !Sub '${MainStackName}-TrackingTableName' }
                - !Sub
                  - 'arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${T}/index/*'
                  - T: { 'Fn::ImportValue': !Sub '${MainStackName}-TrackingTableName' }
            # The main stack's TrackingTable is encrypted with a customer-
            # managed KMS key. DynamoDB transparently requires kms:Decrypt on
            # that key for every Scan/Query the table touches; without this
            # statement boto3 surfaces `AccessDeniedException: KMS key access
            # denied` wrapped in a 500, and the browser shows "Failed to fetch"
            # (or the payload shown in the feature's UI).
            - Effect: Allow
              Action:
                - kms:Decrypt
                - kms:DescribeKey
              Resource:
                'Fn::ImportValue': !Sub '${MainStackName}-CustomerManagedEncryptionKeyArn'
      Events:
        # NOTE: Method must NOT be ANY. When the route is `ANY /{proxy+}`, it
        # matches OPTIONS too, so API Gateway's built-in CORS preflight
        # handler (enabled via CorsConfiguration above) is bypassed — the
        # OPTIONS preflight ends up at the JWT authorizer, which rejects it
        # with 401 and the browser reports "Failed to fetch". Listing only the
        # methods the handler actually serves lets the gateway answer OPTIONS
        # itself with a 204 + CORS headers before any auth check runs.
        Root:
          Type: HttpApi
          Properties:
            ApiId: !Ref FeatureApi
            Path: /{proxy+}
            Method: GET

  ###########################################################################
  # UI deployer: copies the UMD bundle + registers the feature via AppSync.
  # Identical to feature-template/ui-deployer.
  ###########################################################################
  UiDeployerRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Tags:
        - Key: 'idp:feature-id'
          Value: !Ref FeatureId
      Policies:
        - PolicyName: ReadSellerBucket
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: s3:GetObject
                Resource: !Sub 'arn:${AWS::Partition}:s3:::${SellerBucket}/features/${FeatureId}/*'
              # ListBucket is required so that when the source key is missing
              # (e.g. the new version's ui-bundle.js wasn't published yet) S3
              # returns a clear NoSuchKey error instead of the misleading
              # "AccessDenied (s3:ListBucket)" surfaced in the
              # RegisterFeatureResource UPDATE_FAILED CloudFormation event.
              # Scoped to features/<id>/* via condition so the role can't
              # enumerate other publishers' features in a shared bucket.
              - Effect: Allow
                Action: s3:ListBucket
                Resource: !Sub 'arn:${AWS::Partition}:s3:::${SellerBucket}'
                Condition:
                  StringLike:
                    s3:prefix:
                      - !Sub 'features/${FeatureId}/'
                      - !Sub 'features/${FeatureId}/*'
        - PolicyName: WriteWebUIBucketPrefix
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:PutObjectAcl
                  - s3:DeleteObject
                Resource: !Sub
                  - 'arn:${AWS::Partition}:s3:::${Bucket}/features/${FeatureId}/*'
                  - Bucket: { 'Fn::ImportValue': !Sub '${MainStackName}-WebUIBucketName' }
        - PolicyName: InvokeAppSync
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: appsync:GraphQL
                Resource:
                  - !Sub
                    - '${ApiArn}/types/Mutation/fields/registerFeature'
                    - ApiArn: { 'Fn::ImportValue': !Sub '${MainStackName}-AppSyncApiArn' }
                  - !Sub
                    - '${ApiArn}/types/Mutation/fields/unregisterFeature'
                    - ApiArn: { 'Fn::ImportValue': !Sub '${MainStackName}-AppSyncApiArn' }

  UiDeployerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ui-deployer/
      Handler: handler.lambda_handler
      Role: !GetAtt UiDeployerRole.Arn
      Environment:
        Variables:
          FEATURE_ID: !Ref FeatureId
          FEATURE_DISPLAY_NAME: !Ref FeatureDisplayName
          # Version is substituted at publish time — see top-of-file note
          # on why this is not a CFN parameter.
          FEATURE_VERSION: '<FEATURE_VERSION_TOKEN>'
          MAIN_STACK_NAME: !Ref MainStackName
          WEBUI_BUCKET: { 'Fn::ImportValue': !Sub '${MainStackName}-WebUIBucketName' }
          SELLER_BUCKET: !Ref SellerBucket
          SELLER_BUCKET_REGION: !Ref SellerBucketRegion
          APPSYNC_API_URL: { 'Fn::ImportValue': !Sub '${MainStackName}-AppSyncApiUrl' }
          FEATURE_API_ENDPOINT: !GetAtt FeatureApi.ApiEndpoint
          LOG_LEVEL: !Ref LogLevel

  RegisterFeatureResource:
    Type: AWS::CloudFormation::CustomResource
    Properties:
      ServiceToken: !GetAtt UiDeployerFunction.Arn
      # FeatureVersion changes between releases — including it as a property
      # on the custom resource ensures CFN re-runs the resource on update
      # (custom resources are only re-invoked when at least one property
      # changes). Substituted at publish time.
      FeatureVersion: '<FEATURE_VERSION_TOKEN>'
      FeatureDisplayName: !Ref FeatureDisplayName

Outputs:
  FeatureApiEndpoint:
    Value: !GetAtt FeatureApi.ApiEndpoint
  InstalledVersion:
    Value: '<FEATURE_VERSION_TOKEN>'
